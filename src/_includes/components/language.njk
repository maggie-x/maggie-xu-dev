{% macro language(name, iconUrl) %}
    <div class="inline-flex space-x-2 items-center border-2 border-purple-400 text-xl text-center p-4 sm:rounded-xl bg-purple-600 bg-opacity-10 shadow bg-purple-600 bg-opacity-10 shadow bg-purple-600 bg-opacity-10 shadow">
        <div class="box-border w-8 h-auto">
            <img class="object-cover" src="{{iconUrl}}">
        </div>
        <div>{{name}}</div>
    </div>
{% endmacro %}