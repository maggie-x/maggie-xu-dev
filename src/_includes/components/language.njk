{% macro language(name, iconUrl) %}
    <div class="inline-flex space-x-2 items-center border-2 border-gray-700 hover:border-green-400 text-xl text-center p-4 rounded-2xl bg-mint-dark bg-opacity-50 bg-mint-dark bg-opacity-50 bg-mint-dark bg-opacity-50">
        <div class="box-border w-8 h-auto">
            <img class="object-cover" src="{{iconUrl}}">
        </div>
        <div>{{name}}</div>
    </div>
{% endmacro %}